'use client';
import React from "react";

const AddToCart = () => {
    return (
        <div>
            <button onClick={() => console.log("hey")}>Add to card</button>
        </div>
    )
}

export default AddToCart